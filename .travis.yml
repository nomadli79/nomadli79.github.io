sudo: false
language: node_js
node_js: stable
cache: npm
branches:
  only:
    - source
script:
  - npm install hexo
  - hexo clean
  - hexo g
deploy:
  provider: pages
  skip-cleanup: false
  github-token: $GH_TOKEN
  keep-history: false
  on: master
  local-dir: public